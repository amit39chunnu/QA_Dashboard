<aura:component controller="Contact_Data">
    <aura:attribute name="columns" type="list" />
    <aura:attribute name="contacts" type="contact[]" />
    <aura:handler name="init" value="{!this}" action="{!c.show}" />
    <lightning:dataTable columns="{!v.columns}" data="{!v.contacts}" keyField="Id" >
    	
    </lightning:dataTable>
</aura:component>